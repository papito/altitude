<!DOCTYPE html>
<html lang="en">
<head>
    <title>ALTITUDE</title>

    ${include("includes/header_common.ssp")}

    <style>
        #importOptions {
            display: grid;
            height:  100%;
            grid-template-columns: 1fr auto 1fr;
            place-items: center;
        }

        #browseForFiles {
            grid-column: 2;
            width: 100%;
            display: grid;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            height: 170px;
            font-weight: bold;
            text-transform: uppercase;
            background-color: #257008;
            place-items: center;
        }
    </style>
</head>

#import(software.altitude.core.RequestContext)

<body>

<div>
    ${include("includes/nav.ssp")}
</div>

<main>
    <div id="importOptions">
        <div id="browseForFiles">
            <form hx-post="/import/upload" hx-target="#upload-status" enctype="multipart/form-data">
                <input type="file" name="files" id="files" multiple>
                <button type="submit" hxz-post="submit">Upload</button>
            </form>
            <div id="upload-status" hx-get="/upload-status"></div>
        </div>
    </div>
</main>

<footer>
    <%= RequestContext.account.value.getOrElse("None") %>
</footer>

<script lang="js">
    highlightNav("import");
</script>

</body>
</html>
