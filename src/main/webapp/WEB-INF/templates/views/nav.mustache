<style>
    nav {
        display: grid;
        grid-template-columns: 150px repeat(4, 1fr) 250px;
        grid-column-gap: 10px;
        background: #333;
        color: #fff;
        align-items: center;
        box-shadow: rgba(255, 255, 255, 0.07) 0 1px 0;
        border-bottom: 2px solid #272727;
        text-shadow: rgba(0,0,0,0.75) 0 1px;
    }
    nav span, nav a {
        color: #FFF;
        text-decoration: none;
        font-size: .9em;
        font-family: var(--font);
        text-transform: uppercase;
    }
    nav .brand a {
        letter-spacing: .15em;
        text-transform: uppercase;
        text-decoration: none;
        color: #979393;
        font-weight: bold;
        font-size: 1.3em;
    }
    nav div {
        display: flex;
        align-items: center;
        justify-content: center;
    }
    nav .menu {
        letter-spacing: .10em;
        color: #9399b4;
        cursor: pointer;
    }
    nav .menu.import {
        letter-spacing: .1em;
        color: #6ef13c;
    }
    nav input {
        border-radius: .4em;
        color: white;
        border: 2px groove #5a5855;
        padding: .3em;
        background-color: #666562;
        width: 100%;
    }
    nav input::placeholder {
        color: #a5a2a2;
    }

    nav .menu span {
        margin-left: .5em;
    }
</style>

<nav>
    <div class="brand">
        <a href="/">Altitude</a>
    </div>
    <div class="menu">
        <i class="fas fa-database"><a href=""><span>Default</span></a></i>
    </div>
    <div class="menu import">
        <i class="fas fa-download"
           @click="showDirBrowseModal = true"><span>Import</span></i>
    </div>
    <div class="menu">
        <i class="fas fa-search"><a href=""><span>Advanced Search</span></a></i>
    </div>
    <div class="menu">
        <i class="fas fa-adjust"><a href=""><span>Settings</span></a></i>
    </div>
    <div class="search">
        <label for="search"></label>
        <input id="search" type="text" name="search" placeholder="Search">
    </div>
</nav>
