<% import software.altitude.core.Const %>
<% import software.altitude.core.Api %>
<% import software.altitude.core.AltitudeServletContext %>
<% import software.altitude.core.RequestContext %>
<% import software.altitude.core.util.SearchResult %>
<% import software.altitude.core.util.SearchSort %>
<% import software.altitude.core.models.Person %>
<%@ val results: SearchResult %>
<%@ val sort: SearchSort = results.sort.head %>
<%@ val person: Person = null %>

<style>
  #searchControl {
    margin: var(--control-vertical-space);
    padding: 5px;
    border: 1px solid var(--background-tertiary-color);
    border-radius: 5px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
  }

  #searchControl div {
    display: grid;
    place-items: center;
  }

  #searchControl .total {
    color: var(--faded-font-color);
  }

  #assets .cell {
    margin: 3px;
    border: var(--result-border);
    touch-action: none;
  }

  #assets .cell img {
    display: block;
    cursor: pointer;
  }

  #assets {
    padding: 0 10px 10px 10px;
    display: flex;
    flex-wrap: wrap;
    flex-direction: row;
    justify-content: center;
  }

  #assets .cell {
    display: grid;
    width:<%= software.altitude.core.AltitudeServletContext.app.config.getInt(Const.Conf.PREVIEW_BOX_PIXELS).toInt + 20 %>px;
    height:<%= software.altitude.core.AltitudeServletContext.app.config.getInt(Const.Conf.PREVIEW_BOX_PIXELS).toInt + 20 %>px;
    place-items: center;
  }

  .sort-dropdown {
    padding: 5px 25px 5px 5px;
    background-color: var(--background-secondary-color);
    color: var(--faded-font-color);
    border: 1px solid var(--background-tertiary-color);
    border-radius: 5px;
    cursor: pointer;
    letter-spacing: 1px;
    text-transform: uppercase;
    font-size: smaller;
    outline: none;
    appearance: none;
    background-image: url("data:image/svg+xml;utf8,<svg fill='gray' height='24' viewBox='0 0 24 24' width='24' xmlns='http://www.w3.org/2000/svg'><path d='M7 10l5 5 5-5z'/></svg>");
    background-repeat: no-repeat;
    background-position: right 5px center;
  }

  .sort-dropdown:hover {
    background-color: var(--background-tertiary-color);
  }

  .sort-dropdown:focus {
    box-shadow: 0 0 2px 2px rgba(0, 0, 0, 0.1);
  }

  .sort-dropdown span {
    font-size: 1.2em;
  }

</style>

#if (person != null)
<% include("../htmx/person.ssp") %>
#end

<div id="searchControl">
  <div class="total">
    ${results.total} items
  </div>
  <div>
    size
  </div>

  <div class="sort">
    <select id="sortOptions"
            name="sort"
            class="sort-dropdown"
            hx-swap="innerHTML"
            hx-target="#content"
            hx-get="/htmx/search/r/${RequestContext.getRepository.persistedId}"
            hx-trigger="change">
      <option value="created_at1"
        #if (sort.field == "created_at" && sort.direction.id == 1)
              selected
        #end>Date Added (newest first)
      </option>
      <option value="created_at0"
        #if (sort.field == "created_at" && sort.direction.id == 0)
              selected
        #end>Date Added (oldest first)
      </option>

      <option value="original_created_at1"
        #if (sort.field == "original_created_at" && sort.direction.id == 1)
              selected
        #end>Date Created (newest first)
      </option>
      <option value="original_created_at0"
        #if (sort.field == "original_created_at" && sort.direction.id == 0)
              selected
        #end>Date Created (oldest first)
      </option>

      <option value="filename0"
        #if (sort.field == "filename" && sort.direction.id == 0)
              selected
        #end>File Name (A-Z)
      </option>
      <option value="filename1"
        #if (sort.field == "filename" && sort.direction.id == 1)
              selected
        #end>File Name (Z-A)
      </option>
      <option value="dimensions-desc">Dimensions (largest first)</option>
      <option value="dimensions-asc">Dimensions (smallest first)</option>
    </select>
  </div>
</div>

<div id="assets">
  <% include("../htmx/results_grid.ssp") %>
</div>

<script type="module">
  import { context } from "../../../../js/context.js"
  import {
      initInfiniteScroll,
      initLazyLoad,
} from "../../../../js/search-results/infinite-scroll.js"

  // this container template is loaded on new search, so scroll to top
  const contentElement = document.getElementById("content")
contentElement.scrollTo({ top: 0, behavior: "auto" })

  initLazyLoad()
  initInfiniteScroll()

  context.setRepoId("<%= RequestContext.getRepository.persistedId %>")
</script>

<script type="module" src="/js/search-results/dragon-drop.js"></script>
<script type="module" src="/js/search-results/event_handlers.js"></script>
