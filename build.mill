//| mill-version: 1.0.3
package build
import mill._, scalalib._, publish._

object altitude extends ScalaModule with PublishModule {
  def scalaVersion = "3.6.3"
  def jvmVersion = "21"

  def publishVersion = "0.1.0-SNAPSHOT"
  def pomSettings: T[PomSettings] = PomSettings(
    description = "Altitude DAM",
    organization = "altitude-dam",
    url = "https://github.com/papito/altitude",
    licenses = Seq(License.MIT),
    versionControl = VersionControl.github("papito", "altitude"),
    developers = Seq.empty
  )

  def mvnDeps = Seq(
    mvn"com.softwaremill.sttp.tapir::tapir-core:1.11.41",
    mvn"com.softwaremill.sttp.tapir::tapir-netty-server-sync:1.11.41",
    mvn"ch.qos.logback:logback-classic:1.5.16"
  )

  object test extends ScalaTests with TestModule.ScalaTest {
    def mvnDeps = Seq(
      mvn"com.softwaremill.sttp.tapir::tapir-sttp-stub-server:1.11.41",
      mvn"org.scalatest::scalatest:3.2.19",
      mvn"com.softwaremill.sttp.client3::jsoniter:3.10.2"
    )
  }
}
